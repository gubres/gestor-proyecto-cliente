{% extends 'base.html.twig' %}

{% block title %}Edit Proyectos{% endblock %}

{% block body %}
    <h1>Editar Proyectos</h1>

    {{ include('proyectos/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_proyectos_index') }}" class="btn btn-secondary">Back</a>

    {{ include('proyectos/_delete_form.html.twig') }}
{% endblock %}
